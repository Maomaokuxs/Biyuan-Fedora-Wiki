# 说明

由于你使用的是 Fedora，系统默认采用的是 zram（内存压缩技术）作为交换空间，而内存是断电即清空的，所以默认状态下 Fedora 根本无法休眠。不仅如此，Fedora 默认使用的是 btrfs 文件系统，在 btrfs 上直接用普通命令（如 dd 或 fallocate）建交换文件会导致文件系统损坏或性能骤降。可以使用 btrfs 提供的命令。

在使用休眠时，Fedora 提供的内核似乎并不支持开启安全启动并使用休眠，硬核一点的话可以自己重新编译内核，下文中不涉及，默认将安全启动关闭，如果非常需要使用安全启动的话，看自己的取舍。

## 0. 关闭安全启动

进入 BIOS 关闭安全启动。

```bash
mokutil --sb-state
```

需要输出为： SecureBoot disabled 再进行下面的步骤。

## 1. 创建休眠文件

```bash
# 1. 在根目录下创建一休眠文件，大小通常为内存的 1-1.5倍
sudo btrfs filesystem mkswapfile --size 34G /swapfile

# 2. 激活这个交换文件
sudo swapon /swapfile

# 3. 验证是否挂载成功 (可以看到 /swapfile）
swapon --show
```

## 2. 配置开机自动挂载

```bash
# 1.备份当前的 fstab 文件
sudo cp /etc/fstabn /etc/fstab.bak

# 2.将配置追加写入 fstab 文件
echo '/swapfile none swap defaults 0 0' | sudo tee -a /etc/fstab
```

## 3. 配置休眠文件位置

当电脑从休眠中醒来时，内核需要知道去硬盘的哪个分区的哪个位置读取那 32GB 的数据。需要获取两个关键值：UUID 和 物理偏移量 (Offset)。

```bash
# 1. 查找所在的硬盘 UUID (复制输出的那一串字母数字，比如 1234abcd-...)
findmnt -no UUID -T /swapfile

# 2. 查找 Btrfs 文件的物理偏移量 (复制输出的那个纯数字，比如 123456)
sudo btrfs inspect-internal map-swapfile -r /swapfile
```

## 4. 将坐标烧录进内核启动参数

```bash
sudo grubby --update-kernel=ALL --args="resume=UUID=你的UUID resume_offset=你的偏移量"

# 例如：sudo grubby --update-kernel=ALL --args="resume=UUID=c8f8b... resume_offset=53462"
```

## 5.重构内核引导镜像

```bash
# 1. 告诉 Dracut 加上 resume (恢复) 模块
echo 'add_dracutmodules+=" resume "' | sudo tee /etc/dracut.conf.d/resume.conf

# 2. 强制重新生成当前的内核引导镜像 (这一步也需要一点时间)
sudo dracut -f
```

## 6.重启电脑

重启后使用下面的命令测试休眠

```bash
systemctl hibernate
```
