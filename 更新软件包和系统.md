# 说明

- 建议在安装任何软件包之前建议先更新软件包。
- 更新系统可能有一些软件包不兼容，可以先卸载再更新完之后安装

## 更新软件包

1. 更改软件源

        通常使用官方的软件源没有什么问题，由于 Metalink 需要从国外的 Fedora 项目服务器上获取元信息，所以对于校园内网、无国外访问等特殊情况，metalink 并不适用，此时可以如下修改配置文件。

        ```bash
        # 1.备份官方软件源

        sudo cp /etc/yum.repos.d/fedora.repo /etc/yum.repos.d/fedora.repo.bak
        sudo cp /etc/yum.repos.d/fedora-updates.repo /etc/yum.repos.d/fedora-updates.repo.bak

        # 2.更新本地缓存
        sudo dnf makecache

        # 3.替换软件源
        sudo sed -e 's|^metalink=|#metalink=|g' \
            -e 's|^#baseurl=http://download.example/pub/fedora/linux|baseurl=http://mirrors.tuna.tsinghua.edu.cn/fedora|g' \
            -i.bak \
            /etc/yum.repos.d/fedora.repo \
            /etc/yum.repos.d/fedora-updates.repo
        ```

2. 检查可更新的软件包（非必须，但可预览）

        ```bash
        sudo dnf check-update
        ```

3. 升级所有已安装的软件包（核心命令）

        ```bash
        sudo dnf upgrade
        # upgrade 与 update 在 DNF 中作用相同，建议用 upgrade。

        # （可选）升级完成后可用 sudo dnf autoremove 自动卸载无用的依赖包。
        ```

---

## 跨版本升级系统

1. (可选) 生成 snapper 快照

        可以实现系统回滚，更新完成后可以使用旧内核进入系统，将根目录回滚后删除新的内核。

2. 安装必要插件

        ```bash
        sudo dnf install dnf-plugin-system-upgrade
        # 默认安装
        ```

3. 下载新版软件包

        ```bash
        # 将 <版本号> 替换为目标版本，例如 44，需要注意的是对于我来说44版本还是 beta 测试版本。
        sudo dnf system-upgrade download --releasever=44 --enablerepo=fedora --enablerepo=updates-testing
        ```

        `--enablerepo=updates-testing`:用以开启测试仓库，升级一些测试版版本时有用，如果是升级成稳定版可以删除这个参数。

4. 触发重启升级

        ```bash
        sudo dnf offline reboot
        ```

5. 取消升级并删除已经下载的文件

        ```bash
        sudo dnf offline clean
        ```

---
