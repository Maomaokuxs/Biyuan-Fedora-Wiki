# 说明

- 建议在安装任何软件包之前建议先更新软件包。
- 更新系统可能有一些软件包不兼容，可以先卸载再更新完之后安装

## 1.更新软件包

### 1.1 刷新软件源缓存（获取最新软件包列表）

```bash
sudo dnf makecache
```

### 1.2 检查可更新的软件包（非必须，但可预览）

```bash
sudo dnf check-update
```

### 1.3 升级所有已安装的软件包（核心命令）

```bash
sudo dnf upgrade
# upgrade 与 update 在 DNF 中作用相同，建议用 upgrade。

# （可选）升级完成后可用 sudo dnf autoremove 自动卸载无用的依赖包。
```

## 2.跨版本升级

### 2.0. (可选) 生成 snapper 快照

可以实现系统回滚，更新完成后可以使用旧内核进入系统，将根目录回滚后删除新的内核。

### 2.1 安装必要插件

```bash
sudo dnf install dnf-plugin-system-upgrade
```

### 2.2 下载新版软件包

```bash
# 将 <版本号> 替换为目标版本，例如 43
sudo dnf system-upgrade download --releasever=43
```

### 2.3 触发重启升级

```bash
sudo dnf system-upgrade reboot
```
