# 说明

暂时这里没有图形化安装器的指南，我可能会加上，暂时只有文本的说明，不是很直观的话可以看在 B 站发布的视频，up 主是dnf-笔原。

## 1.制作启动U盘并启动

我建议使用 [Rufus](https://rufus.ie/zh/) ，如果是在 windows 使用，在官网下载 .exe 后缀的文件安装后，将 U 盘插上电脑,打开图像化界面后使用默认的配置，选择将要使用的 U 盘，注意这将会格式化 U 盘，先将里面的重要数据备份，接着直接将下载好的 Fedora 镜像复制进 U 盘，重启系统后进入 bios 中将首个启动项设置为 U 盘，建议在 windows 中关闭快速启动，在 bios 中关闭安全启动。进入图形化界面之后选择 Fedora镜像文件就可以引导启动了。

官方也提供有镜像烧录工具[Fedora Media Writer](https://docs.fedoraproject.org/en-US/fedora/latest/preparing-boot-media/#fedora_media_writer)，使用方法可以查看[官方文档](https://docs.fedoraproject.org/en-US/fedora/latest/preparing-boot-media/)。

## 2.进入安装器

通常在桌面只有一个启动器图标，双击启动。

## 3.欢迎

选择简体中文，键盘选择英语（美国）。

## 4.日期和时间

默认即可，一般使用时间服务器自动设置日期与时间，要关注的是时区，在安装的时候选择 Asia/shanghai，上面时间和日期不对的话会在后续装好系统之后会自动同步。

## 4.安装方案

目标位置选择需要使用到的磁盘，不需要使用的不建议勾选，如果选择使用 windows 和 linux双系统，我建议一块磁盘安装一个系统，当然 ntfs 格式的文件系统的分区 linux 也可以挂载，可读也可写，但不建议在当前的操作系统上管理对方的分区，为了避免麻烦。

安装方式的选择上因情况而定。

与其他操作系统共享磁盘，可以先在目标磁盘上空闲出未分配的空间，这样安装器会根据空闲空间的大小智能的划分分区。

使用整个磁盘，除非你整个磁盘都是空的，或者整块磁盘上的文件都不再需要了，否则我强烈的不建议你使用这种方式。

挂载点配置，其中最灵活的方案，与右上角的菜单中存储编辑器搭配最佳，前提是需要知道 linux 系统需要哪一些分区，再者一定要分清楚哪些是需要的分区，在存储编辑器上任何的操作都是立刻进行的，没有反悔的余地，建议略读一下[[linux的文件系统]]，在这一节中我推荐了两种分区方案。

## 5.存储配置

使用整个磁盘方式安装的话使用手动挂载创建好的分区。

## 6.创建账户

全名和用户名一般一致，密码自行这是，下面启动 root 账户建议开启，随后设置密码。

## 7.检查并安装

一定要仔细检查，数据无价，如果有误在那个步骤中修改再重新核对。

随后勾选并点击应用挂载点分配并安装即可。
