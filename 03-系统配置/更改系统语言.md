
# 说明

设置系统语言为简体中文，使桌面环境中使用中文，而在 tty 中自动使用英文（避免显示方块字）。

> tty 不支持 CJK 字符集渲染，如果使用中文 locale 会出现乱码。Fedora 已通过 `/etc/profile.d/lang.sh` 自动处理此问题，无需手动干预 tty 的语言设置。

---

## 1. 设置系统语言

```bash
sudo localectl set-locale LANG=zh_CN.UTF-8
```

此命令会写入 `/etc/locale.conf`，等效于手动编辑：

```bash
sudo vim /etc/locale.conf
```

内容：

```text
LANG="zh_CN.UTF-8"
```

---

## 2. tty 与 GUI 语言自动分离

Fedora 的 `/etc/profile.d/lang.sh` 在检测到当前是虚拟终端 (tty) 时，会自动将 `zh*` / `ja*` / `ko*` 等 locale 强制改为 `en_US.UTF-8`：

```bash
if [ "${TERM}" = 'linux' ] && /usr/bin/tty | grep -q '/dev/tty'; then
    case ${LANG} in
        zh*)    LANG=en_US.UTF-8 ;;
    esac
fi
```

这样 tty 中显示英文，GUI 保持中文，不需要用户额外配置。

---

## 3. 非桌面环境登录管理器下的特殊处理

如果使用 greetd（或其他非 GDM/SDDM 的登录管理器），`niri-session` 启动时会通过 login shell 运行 lang.sh，此时进程仍然连着 tty，lang.sh 会将 LANG 改写为 `en_US.UTF-8`。

随后 `niri-session` 执行 `systemctl --user import-environment`，将已被改写的 `en_US.UTF-8` 导入整个用户会话，导致桌面环境也变成英文。

修复方式：在 `~/.bashrc` 中显式设置 LANG，覆盖 lang.sh 的改写。

```bash
export LANG=zh_CN.UTF-8
```

`.bashrc` 在 lang.sh 之后执行，因此设置的值不会被覆盖。

---

## 4. 配置 CJK 字体

系统默认使用 Noto Sans CJK 作为 sans-serif 后备字体，但它的汉字字形是日语变体，不符合简体中文书写习惯。

使用更纱黑体（Sarasa Gothic SC）替代：

1. 安装更纱黑体：

    ```bash
    mkdir -p ~/.local/share/fonts
    wget -O /tmp/Sarasa-SuperTTC.ttc.tar.gz https://github.com/be5invis/Sarasa-Gothic/releases/latest/download/Sarasa-SuperTTC.ttc.tar.gz
    tar -xf /tmp/Sarasa-SuperTTC.ttc.tar.gz -C ~/.local/share/fonts/
    fc-cache -f
    ```

2. 创建 fontconfig 配置文件 `~/.config/fontconfig/fonts.conf`：

    ```xml
    <?xml version="1.0"?>
    <!DOCTYPE fontconfig SYSTEM "fonts.dtd">
    <fontconfig>
      <alias>
        <family>sans-serif</family>
        <prefer>
          <family>Sarasa Gothic SC</family>
        </prefer>
      </alias>
      <alias>
        <family>monospace</family>
        <prefer>
          <family>Sarasa Mono SC</family>
        </prefer>
      </alias>
    </fontconfig>
    ```

    `<prefer>` 指令会在匹配字族时优先使用指定的字体，使其排在 Noto Sans CJK 之前。

---

## 5. 验证

```bash
# 检查系统 locale
localectl status

# 检查当前 shell locale
locale | head -1

# 检查 systemd 用户环境
systemctl --user show-environment | grep LANG

# 检查 CJK 字体匹配
fc-match -s sans-serif:lang=zh | head -3
```

重新登录后生效。

---

- 参考文档：

  - [Sarasa Gothic](https://github.com/be5invis/Sarasa-Gothic)
