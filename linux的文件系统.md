# 说明

笔者写这一篇文章是用于在安装 linux 系统时能有更多的文件系统的选择，一般要配合介绍相关目录的功能才有更好的效果，我后续因该也会有涉及，介绍 linux 和 windows 的区别是为了帮助读者更容易有个基准去思考linux的文件系统是怎么样的。

## 1. (略读) Linux引用文件和目录与 Windows有什么明显的区别

- linux 路径中不使用驱动器盘符，linux会将文件存储在虚拟目录（virrual directory）的单个目录结构中，虚拟目录会将计算机所有存储设备的文件路径都纳入单个目录结构，只包含一个称为根（root）的基础目录，所有的磁盘分区、光驱、U盘，甚至是网络共享目录，都必须`挂载`到根目录的`挂载点`下,路径本身并没有提供任何有关文件存放在哪个物理磁盘中的信息；在 windows 中使用，所以安装的物理驱动器的分区决定了文件路径，每一个物理磁盘分区都分配一个盘符如C：，即为C盘。形象点说根目录下是一棵树，盘符是一片森林。
  
  - `挂载`：将物理磁盘的分区或者整块物理磁盘对应到根目录下的某一个目录下。
  - `挂载点`：根目录下的挂载着某块磁盘或磁盘分区的某个目录，如/home。

- 在 linux 中使用正斜线（/）来分隔啊文件路径的目录。windows中使用的是反斜线（\）。
  
  - 冷知识： 现代 Windows API 其实能识别正斜杠，但在命令提示符（CMD）或脚本中，反斜杠依然是默认规则。

- linux中大小写敏感，Document.txt、document.txt 和 DOCUMENT.TXT 是三个完全不同的文件。windows大小写不敏感，上面三个文件是同一个文件。

- 隐藏文件的实现方式，Linux中极其简单。只要文件名以点 (.) 开头，它就是隐藏的。Windows中隐藏是一种文件属性。你需要通过文件资源管理器的“属性”勾选“隐藏”。

- Linux中硬件设备、进程信息、网络套接字在文件系统中都有对应的“引用地址”，如引用硬盘中/dev/sda，引用终端：/dev/pts/0。Windows: 硬件设备通常通过设备管理器和注册表进行抽象。

- Windows中主要是 .lnk 文件。它是一个包含目标路径的小文件，如果程序不支持处理 .lnk，它就只是个普通文件。Linux中 拥有原生支持的符号链接 (Symlink) 和 硬链接 (Hard Link)。

  - `符号链接`： 类似于快捷方式，但它是系统层级的。任何程序访问该路径时，文件系统会自动将其重定向到目标地址。

  - `硬链接`： 同一个文件在硬盘上的多个“名字”，删除其中一个，文件内容依然存在，直到所有硬链接都被删除。
  
  - 在[[创建桌面快捷方式.md]]中有相关的快捷方式示例。

## 2. linux文件系统

- 我们称在Linux系统中安装的第一块磁盘为`根驱动器`。根驱动器包含了虚拟目录的核心，其他目录都是从那里开始构建的。

  - linux会使用根驱动器上的特别的目录作为`挂载点`。挂载点是虚拟目录分配给额外存储设备的目录。文件和目录将出现在这些挂载点目录中。

- 系统文件通常存储在根驱动器中；而用户文件则存储在其他驱动器中。
  
  - 常见目录如下

| 目录名称 | 功能说明 |
| --------- | --------- |
| **/** | 根目录，所有目录的起点，整个文件系统的根，通常不会放文件 |
| **/bin** | 二进制可执行文件目录，包含系统启动和运行所需的基本命令（如 ls、cp、bash） |
| **/boot** | 系统引导目录，包含内核文件（vmlinuz）、引导加载器（如 GRUB）配置文件等 |
| **/dev** | 设备文件目录，包含所有硬件设备（如磁盘、终端、USB）的接口文件 |
| **/etc** | 系统配置文件目录，包含系统和应用程序的配置文件（如网络配置、用户信息） |
| **/home** | 用户主目录，每个普通用户在此拥有独立的子目录用于存放个人文件 |
| **/lib** | 系统库文件目录，包含 /bin 和 /sbin 中程序所需的基本共享库和内核模块 |
| **/media** | 可移动介质挂载点，用于自动挂载 CD-ROM、USB 驱动器等可移动存储设备 |
| **/mnt** | 临时挂载点目录，通常用于手动挂载文件系统或网络共享 |
| **/opt** | 可选应用程序目录，通常用于安装第三方或大型应用程序（如某些商业软件） |
| **/proc** | 进程目录，以文件形式提供内核和进程信息的访问接口（内存中生成） |
| **/root** | 系统管理员（root）的主目录，与普通用户的 /home 目录分离 |
| **/run** | 运行目录，存放了很多管理级的 GNU 实用工具 |
| **/sbin** | 系统管理二进制文件目录，包含系统管理员使用的管理命令（如 fdisk、ifconfig） |
| **/srv** | 服务数据目录，用于存储系统服务提供的数据（如网站文件、FTP 数据等） |
| **/sys** | 虚拟文件系统目录，提供内核设备、驱动和系统信息的结构化访问接口 |
| **/tmp** | 临时文件目录，存放程序运行产生的临时文件（系统重启可能清空） |
| **/usr** | 用户程序资源目录，包含用户应用程序、库、文档等（分层结构类似根目录） |
| **/var** | 可变数据目录，存放经常变化的文件（如日志、缓存、邮件、数据库文件等） |

### 补充说明

1. **/usr** 目录下的重要子目录：
   - /usr/bin：用户应用程序的可执行文件
   - /usr/lib：用户应用程序的库文件
   - /usr/local：本地编译安装的软件
   - /usr/share：共享的只读数据（如文档、图标等）

2. **/lib** 与 **/lib64**：
   - 在64位系统中，/lib64 存放64位库文件，/lib 存放32位库文件

3. **现代变化**：
   - 某些发行版中，如fedora，/bin、/sbin 可能已符号链接到 /usr/bin、/usr/sbin
   - /run 目录：替代 /var/run，存放运行时数据（PID 文件等）

4. **FHS标准**：
   这些目录结构遵循文件系统层次标准（Filesystem Hierarchy Standard），确保Linux系统间的一致性。

## 3.推荐分区方案

1. 设备号以实际为准，这里以 SSD（固态硬盘） 为例 。
2. 经测试在 fedora 43 镜像使用的启动器只能将启动分区挂载在 /boot/efi 下。
3. 根目录和家目录下挂载同一块磁盘分区，使用不同的 btrfs 子卷。

- 方案一

| 目录 | 设备 | 文件系统 | 子卷 |
| --------- | --------- | ------ | ------ |
| **/boot/efi** | /dev/nvme0n1p1 | fat32 | |
| **/boot** | /dev/nvme0n1p2 | ext4 | |
| **/** | /dev/nvme0n1p3 | btrfs | @ |
| **/home** | /dev/nvme0n1p3 | btrfs | @home |

- 方案二

| 目录 | 设备 | 文件系统 |
| --------- | --------- | ------ |
| **/boot/efi** | /dev/nvme0n1p1 | fat32 |
| **/boot** | /dev/nvme0n1p2 | ext4 |
| **/** | /dev/mapper/vgroup0-lvol0 | ext4 |
| **/home** | /dev/mapper/vgroup0-lvol1 | ext4 |
