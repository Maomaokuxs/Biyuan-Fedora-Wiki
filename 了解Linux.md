# 说明

事先说明，想使用 Linux 可能需要但不只限于下面的条件，以及下面内容均为主观感受，可能有部分问题，并未有误导意义。

1. 如果只是单纯想尝试，可以现在 Windows 中使用虚拟机安装，如果要在物理机上安装请确保找到可靠的安装视频，现在的 Linux 部分支持使用图形化安装界面，与 Windows 上安装类似，相对于只使用终端命令安装来说简单了很多。
2. 确保有充足的时间，学习使用一个新的事物必然需要花费大量的时间，这是不可避免的，正如刚开始学习使用手机，刚开始使用电脑。
3. 遇到问题有独自解决问题的能力和向他人提问有正确说明问题的能力。
4. 不要对 Linux 系统抱有过多的滤镜，无论是大部分服务器都使用 Linux , 亦或者是 Windows 太垃圾了，我想使用更流畅的 Linux 系统之类的偏见。
5. Linux 系统和 Windows 系统一样只是服务于人的工具，使用工具不要有过多的优越感，但不可否认的是搭建属于自己的系统有不小的成就感。
6. 在内存和硬盘大涨价的现在，确保你至少还有 20 GB 的硬盘空间去安装一个操作系统。
7. 在物理机上安装 Linux 操作系统千万不要盲目的点击下一步以及安装前备份好重要的数据，数据无价。

## 为什么不

为什么要使用使用 Linux ？使用 Linux 有什么意义 ？比 Windows 使用起来更简单吗 ？我觉得行动前想这么多为什么没有必要，有时候想这些越多反而使事情变得更加的麻烦，有时有可以对自己说为什么不，先行动起来，在带入感性中的感受，理性中的思考。你觉得想尝试一下，那就行动起来吧。

## 信创下国产 Linux 操作系统

就目前来说，其推行区域大部分涉及党政，以及金融，电信，石油，电力，交通，航空航天，医院，教育，再往后是其他企业，现在也是按照这种方式去推行，对我印象深刻的国产操作系统有统信 UOS ，麒麟操作系统。因为信创的推行，有部分国产软件也适配了 Linux 操作系统，比如说 QQ ，微信，腾讯会议，搜狗输入法，百度输入法。当然也有使用了玲珑包格式的迅雷，这里提一下，迅雷的功能是阉割版的，腾讯会议也还没有支持 Wayland，两个输入法版本非常的老，并且功能也是阉割的。

虽然软件在生态链来说是最顶层的，但也是普通用户使用一款操作系统感触最深的。现在国产操作系统 Linux 为了简化安装流程，开发了各自应用商店，便于安装需要的软件，这和使用 Windows 安装软件的方式也是不相同的，在国内，大部分应用都需要前往官网下载以 .exe 为后缀的可执行文件，然后一直点下一步，而在 Linux 上不使用终端命令的话，使用软件商店是一个很方便的选择。
国产操作系统界面十分的现代，其性能占用比较高，对于现在国产的CPU，GPU和 内存 来说是不是一个负担我并不好说，但在固态硬盘上其使用体验已经跟上或者超越了外国。

现在寄希望于推出更多外国专业软件相同或超越功能的国产版本,其次就是国内软件在适配上多进分力，当然这个过程通常要伴随着用户数量的增多，或者大量资金的支持。

以上都是根据网上的说法总结，不代表我完全认同这些观点。

## 使用习惯上与 Windows 的不同

我主要使用的是 Fedora Linux，其次还使用过 Ubuntu Linux，Linux mint，Arch Linux，Opensuse tumbleweed Linux，也就是说我并没有使用过国产 Linux 版本。

在 Windows 中大部分的系统设置都可以使用图形化界面，比如控制面板，设置，文件资源管理器，注册表等。而在使用 Linux 大部分使用的是终端去执行命令和修改配置文件，当然当前部分也有替代的图形化界面的来替代终端的操作。在使用图形化界面相比于纯文本界面更符合人的直觉，只要大概明白手机的操作逻辑就能理解电脑，当然不少图形化要素过多，或者说 UI 和手机的差异越来越小，导致有些操作系统的桌面环境被称之为 "大号平板" 。

其次是使用移动存储设备，比如 U 盘，在 Windows 中只要插入了 U 盘，系统就会立即挂载并弹出资源管理器的窗口，在 Linux 通常需要手动点击挂载，并且输入管理员密码。如果在终端中则需要使用对应的命令。

再者如前文所说，Windows 安装各种软件需要在各大官网安装，而 Linux 中通常只需要在终端中执行对应的命令即可，但不得不承认的是专利软件都不能直接在官方仓库中找到了。因此 Arch Linux 的解决办法就是加上社区仓库，当然 Fedora Linux 也有 Copr，RPM Fusion这些仓库，提供一些专利软件，但终究来说专利软件不适配 Linux 实在是太难受。

最后是使用 Linux 除非是特殊版本，主要的系统崩溃源于用户的折腾，当然也有意外的直接导致不可用，现在在我看来，Linux 的稳定性和易用性确实不如 Windows，当使用管了 Windows 的使用习惯后很难接受大部分是终端的场景。

## Linux 在易用性上的改进

添加更多的图形化界面，比如安装器，KDE 桌面环境等，各种桌面环境通常有安装完成后的用户；引导界面，再者就是 Wine ，其为了在 Linux 上使用 Windows 应用程序而被开发，后来也被加入到 Steam ，V 社基于 Arch Linux 开发了 Steam OS，因此在 Linux 慢慢的解决了 Linux 上玩 Windows 游戏的难点，本来我也想买一台 Steam deck，但是属实是性价比不高，除了对于特殊使用场景的玩家有很好的帮助。

其次是启动引导器 Grub ，它支持引导 Windows 和其他 Linux 系统，为双系统提供便利，不用在 Bios 中频繁更改启动项，但有一些 Linux 不支持安全启动，所以需要在设置中关闭安全启动用于引导某些 Linux 系统，但这可能影响 Windows 上需要反作弊的游戏不能运行，这导致在每次开机启动 Windows 时需要开启安全启动。

## Linux 与 Windows 双系统共存

我不认为这是一个很好的选择，如果你不了解硬盘是怎么分区的，极其容易导致 Windows 系统被格式化，又或者说某些 Linux在安装系统时会使用 Windows 系统 EFI 分区，这样极其容易在 Windows 系统更新后删除 Linux 的启动文件，导致无法引导 Linux 系统启动，最终要不就是使用 U 盘重新安装新系统，又或者说是要使用安装镜像来挂载原系统来修复启动分区。

退而求其次来说，至少需要保证两个系统不公用同一个分区，如果要更安全来说，建议使用单独的硬盘来进行安装，也方便不想使用 Linux 是干净的移除。如果还是没有把握，就把安装有 Windows 系统的硬盘移除出电脑，只保留要安装 Linux系统的硬盘。

再者两个系统对于硬件的时间解释不同，Linux 默认将硬件时钟看作 UTC（协调世界时），然后根据时区（比如北京时间 UTC+8）在系统内进行转换成本地时间。
Windows 默认将硬件时钟直接看作 Local Time（本地时间）。这导致每次切换系统 Windows 都要手动调整一下时间，当然也可以修改注册表来让 Windows 使用 UTC。或者让Linux 直接将硬件时间看作是本地时间。

## 正式安装

以 Fedora 安装为例，其他的发行版过程都差不多。

- **制作启动U盘并启动**
  
  我建议使用 ventoy ，如果是在 windows 使用，在官网下载 .exe 后缀的文件安装后，将 U 盘插上电脑,打开图像化界面后使用默认的配置，选择将要使用的 U 盘，注意这将会格式化 U 盘，先将里面的重要数据备份，接着直接将下载好的 Fedora 镜像复制进 U 盘，重启系统后进入 bios 中将首个启动项设置为 U 盘，建议在 windows 中关闭快速启动，在 bios 中关闭安全启动。进入图形化界面之后选择 Fedora镜像文件就可以引导启动了。
  
  官方也提供有镜像烧录工具 Fedora Media Writer。

- **进入安装器**
  
  通常在桌面只有一个安装器图标，双击启动。

- **欢迎**
  
  选择简体中文，键盘选择英语（美国）。

- **日期和时间**
  
  默认即可，一般使用时间服务器自动设置日期与时间，要关注的是时区，在安装的时候选择 Asia/shanghai，上面时间和日期不对的话会在后续装好系统之后会自动同步。

- **安装方案**
  
  目标位置选择需要使用到的磁盘，不需要使用的不建议勾选，如果选择使用 windows 和 Linux双系统，我建议一块磁盘安装一个系统，当然 ntfs 格式的文件系统的分区 Linux 也可以挂载，可读也可写，但不建议在当前的操作系统上管理对方的分区，为了避免麻烦。
  
  安装方式的选择上因情况而定。
  
  - 与其他操作系统共享磁盘，可以先在目标磁盘上空闲出未分配的空间，这样安装器会根据空闲空间的大小智能的划分分区。
  
  - 使用整个磁盘，除非你整个磁盘都是空的，或者整块磁盘上的文件都不再需要了，否则我强烈的不建议你使用这种方式。
  
  - 挂载点配置，其中最灵活的方案，与右上角的菜单中存储编辑器搭配最佳，前提是需要知道 Linux 系统需要哪一些分区，再者一定要分清楚哪些是需要的分区，在存储编辑器上任何的操作都是立刻进行的，没有反悔的余地。

- **存储配置**
  
  使用整个磁盘方式安装的话使用手动挂载创建好的分区。

- **创建账户**
  
  全名和用户名默认设置一致，有需求自行更改；密码自行设置，下面启动 root 账户建议开启，随后设置密码。

- **检查并安装**
  
  一定要仔细检查，数据无价，如果有误在那个步骤中修改再重新核对。
  
  随后勾选并点击应用挂载点分配并安装即可。

## 安装硬件驱动

这是使用 Linux 不得不品的一环，如果显卡只有核显到没什么很大的问题，麻烦是独显，通常 Linux 发行版中不带有专利软件，当然也有自带各种显卡驱动的发行版，这取决于使用哪一款发行版。其次是电脑主板上的其他网卡的等硬件是否能够有相对应的驱动，在往外说，使用的打印机是否也有对应的驱动。这一部分是当前使用 Linux 最大的障碍，这不像是在 Windows 上只要找到厂商的官方网站上去下载可执行文件就可以自动安装。

## Linux 的优势

绝对的自由，只要有相对应的知识，就可以安装自认为习惯的方式去改造这个系统，比如说使用桌面环境或者各种窗口管理器。

轻量，Linux 相较于面面俱到臃肿不堪的 Windows 来说是干净的，没有任何可以干扰你的广告，也没有内置任何软件，你有绝对的对电脑的控制权利。

系统级别快照，使用 btrfs文件系统配合 snapper 软件可以实现系统级别的回档，防止系统的损坏。

## 总结

我本来是想写一篇推荐使用 Linux 的文章，最终尽数写了使用 Linux 的缺点，当然也有开箱即用的 Linux 系统，并且这一切到现在来说大多有替代方案和解决方案，只不过需要花费使用者不少的时间去适应和学习，所以我才会列举这么多麻烦的事情。但如果能接受并折腾这些 Linux 也是一个好系统。
