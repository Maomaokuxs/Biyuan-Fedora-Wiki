
# 说明

- 从按下电源键到看到登录界面，Linux 的启动过程是一个从**底层硬件初始化**到**高层软件服务加载**的精密协作过程。

- 在的现代标准下，我们可以将这一过程分为五个核心阶段。

---

## 固件阶段 (Firmware Stage)

当你按下电源键，主板上的固件（**BIOS** 或 **UEFI**）首先夺取控制权。

* **POST (开机自检)：** 固件会检查 CPU、内存、显卡和硬盘等硬件是否正常工作。
* **查找引导设备：** 固件根据设置好的优先级（如硬盘、USB、网络）寻找包含引导程序的设备。

| 特性 | 传统 BIOS | 现代 UEFI (主流) |
| --- | --- | --- |
| **分区表** | MBR (最大 2TB) | GPT (支持 2TB 以上) |
| **加载方式** | 读取硬盘第一个扇区 (MBR) | 从 EFI 分区 (ESP) 读取 `.efi` 文件 |
| **安全性** | 无 | 支持 Secure Boot (安全启动) |

---

## 引导加载阶段 (Bootloader Stage)

固件找到设备后，会将控制权交给**引导加载程序**（如 **GRUB2** 或 **systemd-boot**）。

* **加载配置：** 引导程序读取配置文件（如 `/boot/grub/grub.cfg`）。
* **用户交互：** 显示启动菜单，让你选择内核版本或不同的操作系统。
* **加载内核：** 引导程序将压缩的**内核镜像 (vmlinuz)** 和**虚拟文件系统 (initramfs)** 加载到内存中。

---

##  内核初始化阶段 (Kernel Stage)

内核被加载到内存并解压后，它开始接管计算机。

* **硬件驱动加载：** 内核探测并初始化剩余的硬件驱动。
* **挂载临时根文件系统：** 内核先挂载 **initramfs**（这是一个内存中的临时根系统），因为它包含了挂载“真正硬盘分区”所需的驱动程序（如加密磁盘驱动、LVM 或 RAID 驱动）。
* **切换根目录：** 一旦真正的磁盘分区准备就绪，内核就会从 initramfs 切换到硬盘上的真实根目录（`/`）。

---

##  初始化系统阶段 (Init System Stage)

这是从“内核空间”向“用户空间”转化的关键点。内核会启动系统中的第一个进程，PID 为 **1**。

* **启动 systemd：** 在现代 Linux 中，这个进程几乎都是 `systemd`。
* **依赖管理：** systemd 会并行启动各种服务（网络、日志、磁盘挂载等）。它根据 **Target**（类似于旧时代的运行级别）来决定启动哪些服务：
* `multi-user.target`: 多用户命令行模式。
* `graphical.target`: 图形界面模式。

---

##  用户登录阶段 (User Space)

当所有必需的服务加载完毕，系统准备好接受用户操作。

* **显示管理器 (Display Manager)：** 如果你使用图形界面，`GDM` 或 `SDDM` 会显示登录窗口。
* **Shell 启动：** 如果是命令行模式，`login` 程序会验证身份并启动用户的 Shell（如 bash 或 zsh）。

---

### 总结：启动流程速记板

> **硬件 (UEFI/BIOS)** $\rightarrow$ **引导 (GRUB)** $\rightarrow$ **内核 (Vmlinuz)** $\rightarrow$ **初始化 (systemd)** $\rightarrow$ **用户 (Login)**
