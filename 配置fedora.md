# 说明

我使用的是 kde 桌面环境，美化通常没有，一般美化的尽头是默认，也是因为实力有限。

## 1.更新系统

```bash
sudo dnf upgrade
# 更新完成后重启系统
```

## 2.安装一些基本软件包

```bash
sudo dnf install vim git fcitx5 fcitx5-chinese-addons fcitx5-configtool
```

## 3.
