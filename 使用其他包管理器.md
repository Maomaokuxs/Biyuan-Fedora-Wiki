# 说明

国内有些包并不存在于 copr rpm 及 fedora 官方仓库中，所以这是一篇用于介绍更多类型的包管理器去使用部分软件。

## Flathub

- Flatpak是什么？

Flatpak 是一个用于在各种 Linux 发行版之间分发桌面应用程序的框架。

Flatpak应用程序本质上是“一次构建，到处部署”的软件包，通过提供自己的依赖运行时环境，确保无论在任何 Linux 发行版上都能以相同的方式运行。

与传统的包管理器不同，Flatpak 采用了一种现代化的应用安全方法，将每个应用运行在独立的安全沙箱中。与 iOS 和 Android 平台类似，这些应用出厂时会带有由各个打包者设定的默认权限集。用户可以随时使用 Flatseal 或命令行选项轻松修改这些权限。

例如，您可以轻松切换互联网或蓝牙连接，限制外围硬件访问，并按应用程序控制哪些文件或文件夹可以被写入或读取。

- Flathub是什么？

Flathub是 Fl​​atpak 的实际中央存储库和应用商店。它拥有超过 100 万活跃用户，包含数千个免费/开源和专有应用程序，其中 2000 多个应用程序已由其上游应用程序开发人员验证（以蓝色勾号表示）。

- 什么是 Flatseal？我为什么需要它？

Flatseal是一个 Flatpak 工具，可以轻松管理应用程序权限，既可以全局管理，也可以针对单个应用程序进行管理。例如，您可以限制 Google Chrome 直接访问敏感文档，或者完全阻止应用程序访问互联网。

虽然许多 Flatpak 应用由其上游开发者维护（例如 Discord、Firefox 等），但也有许多 Flatpak 应用由第三方维护，因此使用时应格外谨慎（例如 Chrome、Spotify 等）。Flatpak 应用的默认权限由其维护者设定，建议在安装后使用 Flatseal 工具检查并根据需要进一步限制这些默认设置。

- 既然 Fedora 自己就提供了 Flatpak，为什么我还需要 Flathub 呢？
Fedora 项目维护着自己的有限 Flatpak 软件仓库，其中的 Flatpak 包在构建时遵循与 Fedora 原生 RPM 包相同的法律和理念限制，不支持受专利保护的编解码器和其他非自由技术。Flathub 应用通常不受此类限制。

Flathub 包含的应用程序也比 Fedora 的软件仓库多得多。

如果您对安全性要求不高，那么 Flathub 的便利性或许值得您做出这样的取舍。

1. 添加 Flathub 完整仓库

    ```bash
    sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
    ```

2. 将 Fedora Flatpak 包替换为 Flathub 上的等效包

    ```bash
    flatpak install --reinstall flathub $(flatpak list --app-runtime=org.fedoraproject.Platform --columns=application | tail -n +1 )
    ```

3. 移除受限的 Fedora 仓库

    ```bash
    sudo flatpak remote-delete fedora
    ```

- 参考文档

  - [Reddit Fedora Wiki Flatpak](https://www.reddit.com/r/Fedora/wiki/index/flatpak/?screen_view_count=5)

## 星火商店

安装完成后将自动创建应用快捷方式，打开有图形化环境供使用。

1. 启用 copr 仓库并安装

    ```bash
    sudo dnf copr enable xmp360/spark-store && sudo dnf install spark-store
    ```

- 参考文档：

  - [星火商店下载](https://www.spark-app.store/download/)

## 如意玲珑

1. 启用仓库并安装

    ```bash
    sudo dnf copr enable mozixun/OpenAtom-Linyaps &&  sudo dnf install linglong-bin linyaps-web-store-installer
    ```

- 参考文档：

  - [安装如意玲珑](https://linyaps.org.cn/guide/start/install.html)
  - [copr:如意玲珑仓库](https://copr.fedorainfracloud.org/coprs/mozixun/OpenAtom-Linyaps/)

## Nix 包管理器

可以在 fedora 上使用 nix 包管理器来安装更多的软件供使用。

1. 配置环境

    ```bash
    # 安装 Fedora 仓库中的 Nix 及其守护进程
    sudo dnf install nix nix-daemon
    
    # 启动并设置开机自启
    sudo systemctl enable --now nix-daemon
    
    # 将自己加入 nixbld 组（可能需要重新登录生效）
    sudo usermod -aG nixbld $USER
    ```

2. 测试环境

    ```bash
    nix shell nixpkgs#hello -c hello
    ```

3. 常用命令

| 功能 | 命令 | 备注 |
| --- | --- | --- |
| 搜索软件 | nix search nixpkgs <关键词> | 在本地索引中查找包名 |
| 安装软件 | nix profile add nixpkgs#<包名> | 类似于 dnf install |
| 查看已安装 | nix profile list | 列出所有通过 profile 安装的包 |
| 卸载软件 | nix profile remove <包名或索引> | 索引号可以通过 list 查看 |
| 升级所有包 | nix profile upgrade '.*' | 升级 profile 中的所有软件包 |

- 参考文档：

  [Changes/Nix package tool](https://fedoraproject.org/wiki/Changes/Nix_package_tool)
