# 说明

当你在 shell CLI 中输入一个外部命令（参见第5章）时，shell 必须搜索系统，从中找到对应的程序，PATH 环境变量定义了用于查找命令和程序的目录，另一种是因为并没有安装相对应的软件包并不属于下面的情况，如果要使用需要安装相对应的软件包。

## 1.整体思路

在$变量中添加命令所在的文件夹。

## 2.查看$PATH变量

```bash
echo $PATH
```

## 3.临时添加命令所在目录

```bash
PATH = $PATH:/usr/sbin
```

重启后将失效

## 4.永久添加命令所在目录

在$HOME/.bashrc文件中添加

```bash
export PATH=$PATH:/usr/sbin
```

## 5.确认目录已经添加至 $ 变量

```bash
echo $PATH
```
