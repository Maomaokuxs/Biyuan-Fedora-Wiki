
### 为什么要暂时关闭压缩？

常见原因包括：
- **排查性能问题**：怀疑压缩过程（尤其是高等级 `zstd`）在 CPU 性能较弱的设备上成为瓶颈。
- **测试原始磁盘性能**：进行基准测试，对比开启和关闭压缩时的差异。
- **处理已压缩文件**：避免对已经高度压缩的文件（如 `.zip`, `.mp4`, `.jpg`）进行无谓的 CPU 消耗。

---

### 方法一：为整个文件系统禁用压缩（需重新挂载）

这是最彻底的方法，会影响该文件系统上的所有子卷。

1.  **首先，找到你的 Btrfs 文件系统的挂载点**。你可以使用 `findmnt` 命令：
 ```bash
findmnt -t btrfs
```

2.  **编辑 `/etc/fstab` 文件**，修改对应的挂载项。
```bash
sudo nano /etc/fstab
```

3.  **找到你的 Btrfs 挂载行**，它可能看起来像这样：
```
 UUID=1234-5678 /mnt/btrfs_disk btrfs defaults,compress=zstd:1 0 0
```

或者

```
/dev/sda2 /mnt/btrfs_disk btrfs defaults,subvol=@,compress=zstd:1 0 0
```

4.  **移除或注释掉压缩选项**。
    - **移除**：直接删除 `,compress=zstd:1` 部分。
    - **注释**：在行首加 `#`，然后复制一行并修改。
    
    修改后应类似：
 ```
UUID=1234-5678 /mnt/btrfs_disk btrfs defaults 0 0
```
4.  **重新挂载文件系统**以应用更改：
```bash
sudo mount -o remount /mnt/btrfs_disk
```
（请将 `/mnt/btrfs_disk` 替换为你的实际挂载点）

**注意**：此操作仅对**新写入**的数据生效。已经用旧压缩设置写入的文件将继续保持压缩状态，直到被重写。

---

### 方法二：为特定目录禁用压缩（无需重新挂载，使用 `chattr`）

如果你只想为某个特定的子卷或目录（例如你的下载目录）禁用压缩，这是更灵活的方法。

Btrfs 使用 `chattr` 命令的 `c` 标志来控制压缩。

1.  **禁用压缩**：为目录设置 `-c` 标志。

```bash
 sudo chattr -c /path/to/your/download/directory
```
**重要**：使用 `-R` 递归参数通常无效且不推荐。这个属性主要影响在该目录下**新创建**的文件和子目录。

2.  **验证属性已更改**：

```bash
 lsattr /path/to/your/download/directory
```
	如果输出中没有显示 `c`，则表示压缩已禁用。

**说明**：
- 使用 `chattr +c` 可以重新启用压缩。
- 此设置是**继承性**的。在该目录下创建的新文件和子目录都会继承这个“不压缩”的属性。
- 同样，这**只影响新写入的数据**。已有文件的压缩状态不变。

---

### 方法三：在挂载时临时禁用

如果你只是想临时测试一下，可以在不修改 `fstab` 的情况下，使用 `mount` 命令重新挂载：

```bash
sudo mount -o remount,compress=none /mnt/btrfs_disk
```

这个改变在下次重启后会失效，系统会重新读取 `fstab` 中的配置。

---

### 总结与建议

| 方法 | 作用范围 | 永久性 | 适用场景 |
| :--- | :--- | :--- | :--- |
| **修改 fstab** | 整个 Btrfs 文件系统 | 永久 | 确定不再需要整个文件系统的压缩 |
| **使用 chattr** | 单个目录及其未来内容 | 永久 | 只为特定目录（如下载、虚拟机）禁用压缩 |
| **临时 remount** | 整个 Btrfs 文件系统 | 临时（重启失效） | 快速测试性能差异 |

**最佳实践建议**：
对于你的“下载”用例，**方法二（使用 `chattr -c`）通常是最佳选择**。因为它针对性强，不会影响系统子卷（如 `/` 或 `/home`）可能从压缩中获得的好处（例如系统日志、配置文件等）。只需对你的下载目录执行一次 `sudo chattr -c /path/to/downloads`，之后所有下载到该目录的新文件都不会被压缩。