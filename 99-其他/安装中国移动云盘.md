# 说明

中国移动云盘（139 云盘）Linux 客户端只提供 `.deb` 包，没有 RPM 版本。以下方法将其解压安装到 Fedora 上。

## 1. 下载

浏览器打开 `https://yun.139.com/w/#/client`，选择 Linux 版下载（`.deb` 包）。

## 2. 安装依赖

```bash
# Electron 主程序不需要额外依赖
# 但下载/上传后端是 Qt5 程序，需要安装系统 Qt5
sudo dnf install -y qt5-qtmultimedia
```

## 3. 安装应用

```bash
# 解压 deb 到本地目录
dpkg-deb -x ~/Downloads/com.cmic.mcloud_*.deb /tmp/mcloud-extract

# 复制到 Applications
mkdir -p ~/Applications/139cloud
cp -a /tmp/mcloud-extract/opt/apps/com.cmic.mcloud/* ~/Applications/139cloud/

# 清理临时文件
rm -rf /tmp/mcloud-extract
```

## 4. 创建桌面条目

```bash
cat > ~/.local/share/applications/com.cmic.mcloud.desktop << 'EOF'
[Desktop Entry]
Name=中国移动云盘
Comment=中国移动云盘客户端
Exec=/home/biyuan/Applications/139cloud/mcloud
Icon=/home/biyuan/Applications/139cloud/resources/app.asar.unpacked/resources/icons/com.cmic.mcloud.png
Terminal=false
Type=Application
Categories=Network;FileTransfer;
StartupWMClass=中国移动云盘
EOF
```

## 5. 启动

应用菜单 → 搜索"中国移动云盘"，或终端运行：

```bash
~/Applications/139cloud/mcloud
```

## 6. 常见问题

- **浏览文件正常，下载失败**：缺少 Qt5 多媒体库，安装 `qt5-qtmultimedia` 后重启应用
- **提示"另一个实例正在运行"**：杀掉残留进程后重试

  ```bash
  pkill -9 mcloud
  ```
